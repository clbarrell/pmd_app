<% provide(:title, 'New Project') %>
<h1>New Project</h1>

<div class="row">
    <!-- Aside link section -->
    <%= render 'linkaside' %>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
</div>