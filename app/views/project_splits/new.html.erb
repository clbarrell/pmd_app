<% provide(:title, 'New Project Split') %>
<div class="container">
  <!-- Heading  -->
  <div class="row">
    <h1>New Split</h1>
  </div>
  <div class="row">
    <!-- Aside link section -->
    <%= render 'linkaside' %>
    <!-- project panel side -->
    <div class="col-md-4 col-md-offset-2">
      <%= render 'form' %>
    </div>
    <!-- Splits Side -->
    <div class="col-md-4" id="splits">
      <h2>Existing Splits</h2>
      <%= render 'splits' %>
    </div>
    <!-- end splits side -->
  </div>
</div>